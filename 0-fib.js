function fib(n,x={}){
    if (...) {
        
    }

    if (exist fn-1) ret;
    if (exist fn-2) ret;

    let result=  fib(n-1) + fib(n-2);
    // add result to x
    return result;
    // Memoization
}